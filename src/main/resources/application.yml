spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hackaton2023
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
logging:
  config: 'classpath:log4j2.yml'